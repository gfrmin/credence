MVP-D spot-check: OpenClaw (the product) run LIVE on real Terminal-Bench tasks, in each task's
own container, graded by the task's OWN tests (oc_tb_spotcheck.sh). Grounds the routing/welfare
result (assembled from the full TB matrix) in the actual product, and validates the matrix —
generated by the claude-CLI agent (an OpenClaw-class native-tool-calling loop) — as a faithful
proxy: OpenClaw reproduces the matrix's per-(model,task) solve pattern exactly.

Run 2026-06-18. Anthropic models. solve = the task's pytest oracle.

  task                 model    result        dur     matrix(claude-CLI)   match
  hello-world          haiku    SOLVED 2/2     19s     (not in matrix)       —
  fix-permissions      haiku    SOLVED 1/1     32s     haiku 1.00            ✓
  heterogeneous-dates  haiku    FAILED 2/3     25s     haiku 0.00            ✓
  heterogeneous-dates  sonnet   SOLVED 3/3     35s     sonnet 1.00           ✓
  sqlite-db-truncate   haiku    FAILED         60s     haiku 0.00            ✓
  sqlite-db-truncate   sonnet   (inconclusive — transient apt outage in the grading step; the
                                 agent ran, but ubuntu-based pytest install couldn't reach
                                 archive.ubuntu.com. het-dates is the clean both-tiers differentiator.)

THE LIVE DIFFERENTIATION (the routing-relevant regime, through the product):
  heterogeneous-dates (medium): OpenClaw+haiku FAILS (computes the wrong average — file format
  right, value wrong); OpenClaw+sonnet SOLVES. So the MODEL CHOICE genuinely decides success on
  a differentiating task — exactly what routing is for. A cost-saver routed to haiku saves money
  but fails it; a quality-first routed to sonnet pays more and gets the answer. credence-pi
  routes per the user's profile.

WHAT THIS GROUNDS:
  - OpenClaw RUNS real TB tasks live, graded by TB's own tests (hello-world, fix-permissions).
  - OpenClaw reproduces the capability×cost matrix's tier split (fix-permissions haiku✓;
    het-dates haiku✗/sonnet✓; sqlite haiku✗) → the claude-CLI matrix is a faithful OpenClaw
    proxy, so the routing dominance scored over it (results/escalation_live.txt) transfers.

HARNESS NOTE: oc_tb_spotcheck.sh builds the task's own image, bind-mounts the host OpenClaw
(2.3GB node_modules), installs a matching node, runs `openclaw agent --model <tier>` in /app,
then runs the task's pytest oracle. No plugin/daemon here (the de-risk already proved plugin +
daemon route+govern live); routing dominance is scored offline over the matrix, the proven
escalation_live.jl method. Ubuntu-based tasks need apt at grade time (occasional mirror flake).
