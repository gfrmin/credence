# Instructions

Your task is to determine whether a given year is a leap year.

Implement your solution in leap.py so that all tests in leap_test.py pass. After editing, run `python3 -m pytest -q` to confirm.
